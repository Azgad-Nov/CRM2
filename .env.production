# =============================================================================
# .env.production — Railway Production Environment Variables (TEMPLATE)
# =============================================================================
# This is a TEMPLATE with placeholder values. DO NOT fill in real secrets here.
# Use the Railway dashboard or CLI to set actual values:
#   railway variables --set "KEY=value" --environment production
# =============================================================================

# ---- Backend Service Variables ----

# Flask session secret — generate with:
#   python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=<generate-a-unique-secret-key>

# Supabase — use a SEPARATE production project from staging
SUPABASE_URL=<your-production-supabase-project-url>
SUPABASE_KEY=<your-production-supabase-anon-key>
SUPABASE_SERVICE_KEY=<your-production-supabase-service-role-key>

# OpenAI — used for RAG chat and churn prediction features
OPENAI_API_KEY=<your-openai-api-key>

# PORT is set automatically by Railway — do not set manually
# PORT=

# ---- Frontend Service Variables ----

# Internal URL of the backend service (Railway internal networking)
# Format: http://<backend-service-name>.railway.internal:<port>
BACKEND_URL=<http://backend.railway.internal:PORT>
